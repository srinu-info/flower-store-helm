namespace: store
deployment:
  replicas: 1
  imageName: devopsrinu/api-gateway
  imageVersion: v1

hpa:
  minReplicas: 1
  maxReplicas: 1
  averageUtilization: 80

service:
  targetPort: 8080
  port: 8080

