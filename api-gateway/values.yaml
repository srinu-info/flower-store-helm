namespace: store
deployment:
  replicas: 1
  imageName: devopsrinu/api-gateway
  imageVersion: v1

hpa:
  minReplicas: 1
  maxReplicas: 3
  averageUtilization: 10

service:
  targetPort: 8080
  port: 8080

