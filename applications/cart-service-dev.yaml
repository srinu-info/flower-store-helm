apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: cart-service-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/srinu-info/flower-store-helm.git
    targetRevision: development
    path: cart-service

    helm:
      valueFiles:
        - values-dev.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: store

  syncPolicy:
    automated:
      prune: true
      selfHeal: true