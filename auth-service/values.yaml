namespace: store
deployment:
  replicas: 1
  imageName: devopsrinu/auth-service
  imageVersion: v1

hpa:
  minReplicas: 1
  maxReplicas: 2
  averageUtilization: 80

service:
  targetPort: 8081
  port: 8081